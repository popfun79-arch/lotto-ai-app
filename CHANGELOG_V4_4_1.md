# Lotto64 v4.4.1 — Seed Import Hotfix

- Streamlit `ImportError` for `FIXED_SEED` 수정
- `lotto64/config.py`에 Seed 상수 명시
- `seed_stability.py` 및 `app.py`에 이전 config 호환 fallback 추가
- CI에서 Seed 설정과 Seed Stability 실제 import 검증 추가
